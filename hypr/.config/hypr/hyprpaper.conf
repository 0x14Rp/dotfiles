# Primero los preloads (siguen siendo líneas simples)
preload = $HOME/Pictures/ix.png

# Bloque de configuración para tu monitor Xiaomi
wallpaper {
    monitor = DP-2
    path = $HOME/Pictures/ix.png
    # fit_mode puede ser: cover, contain, fill o stretch
    fit_mode = cover
}

# Opcional: Un fallback para cualquier otro monitor que conectes
wallpaper {
    monitor = 
    path = $HOME/Pictures/ix.png
}

# Opciones globales
splash = false
ipc = on
